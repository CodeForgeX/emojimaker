# Cloudflare Pages 配置（部署前请填写 account_id 或通过 CLI 参数传入）
name = "emojimaker"
compatibility_date = "2024-04-01"

[env.production.vars]
# 部署时可通过 Cloudflare Dashboard 设置，也可在 CLI 中覆盖
PUBLIC_GTAG_ID = "G-7Z12050E0W"

[pages]
pages_build_output_dir = "./dist"
pages_build_command = "pnpm build"
